<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Dashboard</title>
  <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@200;300;400;600;700;900&display=swap"
    rel="stylesheet" />
  <link href="/tailwind.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/gh/alpinejs/alpine@v2.7.3/dist/alpine.min.js" defer></script>
</head>

<body class="bg-gray-700">
  <div x-data="setup()" x-init="$refs.loading.classList.add('hidden'); setColors(color);" :class="{ 'dark': isDark}">
    <div
      class="bg-gray-900 flex flex-col items-center justify-center min-h-screen p-4 space-y-4 antialiased text-gray-100">
      <!-- Brand -->
      <a class="inline-block mb-6 text-3xl font-bold tracking-wider uppercase text-primary-dark dark:text-light">
        <%= name %>
      </a>
      <main>
        <div class="w-full max-w-sm px-4 bg-gray-700 text-black shadow-xl py-6 space-y-6 rounded-md dark:bg-darker">
          <h1 class="text-xl font-semibold text-center">Login</h1>
          <form action=<%=route%> method="POST" class="space-y-6">
            <input
              class="w-full px-4 py-2 border rounded-md dark:bg-darker dark:border-gray-700 focus:outline-none focus:ring focus:ring-primary-100 dark:focus:ring-primary-darker"
              type="text" name="email" placeholder="Username" required />
            <input
              class="w-full px-4 py-2 border rounded-md dark:bg-darker dark:border-gray-700 focus:outline-none focus:ring focus:ring-primary-100 dark:focus:ring-primary-darker"
              type="password" name="password" placeholder="Password" required />
            <div class="w-full">
              <button
                class="flex w-full items-center my-8 justify-center px-4 py-2 space-x-2 text-white transition-all duration-200 bg-black rounded-md hover:bg-opacity-80 focus:outline-none focus:ring-2 focus:ring-black focus:ring-offset-1 dark:focus:ring-offset-darker"
                type="submit"> Login to The Panel </button>
            </div>
          </form>
          <span class="flex items-center justify-center space-x-2">
            <span class="h-px bg-gray-400 w-14"></span>
            <a href="/signup" class="font-normal text-white">Sign Up as Donor</a>
            <span class="h-px bg-gray-400 w-14"></span>
          </span>
          <span class="flex items-center justify-center space-x-1">
            <span class="h-px bg-gray-400 w-14"></span>
            <a href=<%=aroute%> class="font-normal text-white" >Are you a NGO?</a>
            <span class="h-px bg-gray-400 w-14"></span>
          </span>
        </div>
      </main>
    </div>

</body>

</html>